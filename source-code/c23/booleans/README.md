# Booleans

In C23, `true`/`false`/`bool` are keywords, and `stdbool.h` doesn't need to be included.


## What is it?

1. `booleans.c`: application that illustrates the use of booleans in C23.
1. `CMakeLists.txt`: CMake file to build the application.

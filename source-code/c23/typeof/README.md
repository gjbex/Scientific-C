# typeof

C23 adds the `typeof` operator, which allows you to determine the type of a
variable at compile time. This is particularly useful for generic programming
and metaprogramming.


## What is it?

1. `swap.c`: application that defines a macro to swap two variables of any
   scalar type.
1. `CMakeLists.txt`: CMake file to build the application.

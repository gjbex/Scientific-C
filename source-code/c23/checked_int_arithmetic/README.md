# Checked integer overflow

C23 adds `checked` integer overflow so that this can be detected conveniently.


## What is it?

1. `check_overflow.c`: application that illustrates the use of checked integer
   operations, as well as its performance impact.
1. `CMakeLists.txt`: CMake file to build the application.
